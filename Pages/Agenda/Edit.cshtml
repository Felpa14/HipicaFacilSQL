@page
@model HipicaFacilSQL.Pages.Agenda.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Evento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Evento.ID" />
            <div class="form-group">
                <label asp-for="Evento.Title" class="control-label"></label>
                <input asp-for="Evento.Title" class="form-control" />
                <span asp-validation-for="Evento.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Evento.Start" class="control-label"></label>
                <input asp-for="Evento.Start" class="form-control" />
                <span asp-validation-for="Evento.Start" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Evento.End" class="control-label"></label>
                <input asp-for="Evento.End" class="form-control" />
                <span asp-validation-for="Evento.End" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
