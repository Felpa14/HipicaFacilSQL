@page
@model HipicaFacilSQL.Pages.Cavalos.DetailsModel

@{
    ViewData["Title"] = "Detalhes do Cavalo";
}

<h1>Detalhes do Cavalo</h1>



<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Nome
        </dt>
        <dd class="col-sm-10">
            @Model.Cavalo.Nome
        </dd>
        <dt class="col-sm-2">
            Raça
        </dt>
        <dd class="col-sm-10">
            @Model.Cavalo.Raca
        </dd>
        <dt class="col-sm-2">
            Peso
        </dt>
        <dd class="col-sm-10">
            @Model.Cavalo.Peso
        </dd>
        <dt class="col-sm-2">
            Data de Nascimento
        </dt>
        <dd class="col-sm-10">
            @Model.Cavalo.Idade
        </dd>
        <dt class="col-sm-2">
            Proprietário
        </dt>
        <dd class="col-sm-10">
            @Model.Cavalo.Proprietario
        </dd>
    </dl>
    <dl class="row">
        <dt class="col-sm-6">
            Imagem
        </dt>
        <dd class="col-sm-6">
            <img src="@Model.Cavalo.ImagemPath" alt="Imagem do Cavalo" />
        </dd>
    </dl>
        <dl class="row">
        <dt class="col-sm-6">
            Documentos
        </dt>
        <dd class="col-sm-6">
            @foreach (var docPath in @Model.Cavalo.DocumentosPaths)
            {
                <a href="@docPath" target="_blank">@System.IO.Path.GetFileName(docPath)</a>

                <br />
            }
        </dd>
    </dl>
</div>

<div>
    <a asp-page="./Index"><i class="fa-solid fa-backward-step"></i> Voltar para a tabela </a> |
    <a asp-page="./Edit" asp-route-id="@Model.Cavalo.ID"><i class="fa-solid fa-pen"></i>Editar Cavalo</a> 
   
</div>

<hr />
<h4>QR Code para esta página</h4>
@if (Model.QRCodeBytes != null)
{
    <img class="justify-content-center" src = "data:image/png;base64,@(Convert.ToBase64String(Model.QRCodeBytes))" alt = "QR Code" />
}